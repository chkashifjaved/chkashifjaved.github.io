name: Simple CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  writing_secrets_to_file:
   runs-on: ubuntu-latest
   steps:
    - name: Try
      run: |
        echo $TOKEN
      env: 
        TOKEN: '${{ secrets.AUTH_TOKEN }}'     

